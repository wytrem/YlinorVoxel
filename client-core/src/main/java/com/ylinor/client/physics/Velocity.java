package com.ylinor.client.physics;

import com.artemis.Component;


/**
 * Represents an entity velocity (in the common way meaning, not the physics way).
 */
public class Velocity extends Component {
    public float speed = 0.5f;
}
