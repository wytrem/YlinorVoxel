package com.ylinor.library.api.world.storage;

import com.ylinor.library.api.world.IChunkContainer;
import com.ylinor.library.util.math.PositionableObject3D;

public abstract class StorageManager implements IChunkContainer
{
    public void playerPosUpdate(PositionableObject3D pos)
    {
    }
}
